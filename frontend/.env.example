# Public env (Astro exposes any var prefixed PUBLIC_ to the browser).
# Same shape as backend/.env but with PUBLIC_ prefixes for the browser.

PUBLIC_NEON_AUTH_URL=https://ep-spring-shadow-arth5zci.neonauth.c-4.us-west-2.aws.neon.tech/neondb/auth
# Host only — lib/auth.ts appends /api/v1 to every path.
PUBLIC_API_BASE_URL=http://localhost:8000
