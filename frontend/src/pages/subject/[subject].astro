---
export const prerender = false;

// Subject-only routes carry no chat code, and the welcome composer lives
// strictly on /new — bounce here instead of rendering it twice.
return Astro.redirect("/new");
---
